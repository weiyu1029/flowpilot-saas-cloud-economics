# The current app requires no secrets. Optional future integrations:
# OPENAI_API_KEY = "..."
# ANTHROPIC_API_KEY = "..."
# AWS_ACCESS_KEY_ID = "..."  # Prefer IAM roles in AWS; never commit credentials.
