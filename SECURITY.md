# Security Policy

This repository contains synthetic data only. Do not add real customer PII, AWS access keys, private SSH keys, database passwords, or API credentials.

Use Streamlit Community Cloud secrets or environment variables for optional integrations. The default app requires no secrets.

To report a security issue, open a private security advisory in GitHub rather than a public issue.
